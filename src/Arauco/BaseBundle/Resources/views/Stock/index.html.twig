{% extends '::base.html.twig' %}

{% block title %} Stock sin asignar -{% endblock %}

{% block content %}
<div class="tab-content">
  {% block body %}
  <div class="tab-pane active" id="inicio">
    <div class="hero-unit">
      {% block titulo %}
      <legend>Stock disponible</legend>
      {% endblock %}
      <p>Stock sin asignar = <span class="label label-success">{{ total }}</span> lotes.</p>
      <a class ="text-info" href="{{ path ('arauco_stock_csv') }}">Exportar a .csv <i class="icon-download-alt"></i></a>
      <br />
      <br />
      <table class="table" id="myTable">
        <thead>
          <tr>
            <th>Material</th>
            <th>Descripción</th>
            <th>Volumen Disponible</th>
          </tr>
        </thead>
        <tbody>
          {% for material in stocksa %}
          <tr>
            <th>{{ material.Material }}</th>
            <th>{{ material.Desc_Mat }}</th>
            <th><a href="{{ path('arauco_stock_extend', {'id': material.Material }) }}">{{ material.tm3 | number_format(2) }}</a></th>
          </tr>
          {% endfor %}
        </tbody>
      </table>
      <br />
      <div class="alert alert-info">
        <strong>NOTA</strong>: Todos los Lotes mostrados en esta página están en Almacen 100 y todas las cantidades mostradas en la tabla están en m&sup3;.
      </div>
    </div> <!-- hero-unit -->
  </div> <!-- tab-pane active -->
  {% endblock %}
</div> <!-- tab-content -->
{% endblock %}
